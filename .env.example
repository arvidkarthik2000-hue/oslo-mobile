# OSLO Mobile — Environment
# Copy to .env and update with your LAN IP

# Backend API URL — use your LAN IP, NOT localhost
# Android emulator/device cannot reach host localhost
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
